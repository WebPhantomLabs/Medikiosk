"""
intake.py — placeholder router.

Endpoints for this domain are implemented in a later phase of the MediKiosk
build (see README.md / PRD.md "Implementation Phases"). Registered here now
so application wiring, import paths, and router registration are stable
from Phase 0 onward.
"""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["intake"])
